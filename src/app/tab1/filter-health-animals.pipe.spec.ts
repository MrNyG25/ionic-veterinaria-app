import { FilterHealthAnimalsPipe } from './filter-health-animals.pipe';

describe('FilterHealthAnimalsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHealthAnimalsPipe();
    expect(pipe).toBeTruthy();
  });
});
